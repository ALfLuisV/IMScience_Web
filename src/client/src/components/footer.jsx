import React from 'react';
import { Breadcrumb, Layout, Menu, theme } from 'antd';
const { Footer } = Layout;



const FooterHP = () => {

    return (
        <Layout>
            <Footer
                style={{
                    textAlign: 'center',
                }}
            >
              FOOTER
            </Footer>
        </Layout>
    );
};

export default FooterHP;